
2021-11-08 
login.php

acressentado:
		alterado ----->				$sql = "SELECT * FROM `tbFunc` WHERE `ativo`='s' AND `email` = '$email' AND `senha` = '$senha'";

			$resultado = mysqli_query($conexao,$sql);
			if ($linha = mysqli_fetch_array($resultado)) {
				$_SESSION["idFunc"] = $_linha["idFunc"]; //		<------ foi acressentado esta linha		
				$_SESSION["logado"]=TRUE;
				$_SESSION["funcao"]=$linha["funcao"];
				$_SESSION["nome"]=$linha["nome"];
				header("Location: index.php");

Tabela: tbFunc
acressentado o campo ativo
tipo: char
tamanho: 1


upload.php

				// removendo a imagem antiga
				$nomeFotoAntiga = $_POST["nomeFotoAntiga"];
				if (file_exists("../imagens/".$nomeFotoAntiga)&&($nomeFotoAntiga!=="sem-foto.png")) {
					unlink("../imagens/".$nomeFotoAntiga);
				}				
				header("Location: listarProduto.php");
        	} else {
					echo 'Erro ao salvar o arquivo. Aparentemente você não tem permissão de escrita.<br />';
					echo "<form action='listarProduto.php'>";
					echo "<button type='submit'>Voltar</button>";
					echo "</form>";	
			}
		} 
		
foi acressentado estas linha		
		else {
			echo "<p>Somente são permitidos extensões: .jpg;.jpeg;.gif;.png</p>";
			echo "<form action='listarProduto.php'>";
			echo "<button type='submit'>Voltar</button>";
			echo "</form>";	
			
		}
até aqui		
		
		
	} else {
		header("Location: listarProduto.php");
	}
?>


Alteração no index.php na pasta admin

<?php
	echo "<h1>Seja Bem-Vindo!, ".$_SESSION["nome"]."</h1>";
	if (($_SESSION["funcao"] === "gerente")) {?>
		<form action="listarProduto.php">
		<button type="submit">Proudutos</button>
		</form>
		<p></p>
	<?php }?>
	<form action="listarFuncionario.php">
	<button type="submit">Funcionários</button>
	</form> 
	<p></p>
    <form action="sair.php">
	<button type="submit">sair</button>
	</form>	

Alteração no login.php
$_SESSION["idFunc"] = $linha["idFunc"];				